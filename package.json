{
  "name": "HACK_UDC",
  "version": "1.0.0",
  "description": "Backend para ClothifyHack",
  "main": "app.js", // Punto de entrada de tu aplicación
  "scripts": {
    "start": "node app.js", // Comando para iniciar el servidor en producción
    "build": "echo 'Build completo'", // Comando de construcción (opcional)
    "dev": "nodemon app.js" // Comando para desarrollo local (opcional)
  },
  "dependencies": {
    "express": "^4.18.2", // Framework para crear el servidor
    "cors": "^2.8.5", // Middleware para permitir CORS
    "multer": "^1.4.5-lts.1", // Middleware para manejar archivos
    "dotenv": "^16.0.3" // Para manejar variables de entorno
  },
  "engines": {
    "node": "18.x" // Especifica la versión de Node.js que usas
  }
}
